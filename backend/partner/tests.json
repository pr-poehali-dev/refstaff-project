{
  "tests": [
    {
      "name": "Get profile without token returns 401",
      "method": "GET",
      "path": "/?action=profile",
      "expectedStatus": 401,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    },
    {
      "name": "Get referrals without token returns 401",
      "method": "GET",
      "path": "/?action=referrals",
      "expectedStatus": 401,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    }
  ]
}
